---
import TechCard from '@components/cards/TechCard.astro';
import { useTranslations, defaultLang } from '@i18n/utils';
import type { Language } from '@/i18n/utils';

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const safeLang = lang || defaultLang;
const t = useTranslations(safeLang);

const technologies = [
  { name: 'Azure', category: 'Cloud Platform', icon: '☁️' },
  { name: 'Kubernetes', category: 'Container Orchestration', icon: '⎈' },
  { name: 'Terraform', category: 'Infrastructure as Code', icon: '🏗️' },
  { name: 'Docker', category: 'Containerization', icon: '🐳' },
  { name: 'Python', category: 'Programming', icon: '🐍' },
  { name: 'TypeScript', category: 'Programming', icon: '📘' },
  { name: 'AI/ML', category: 'Artificial Intelligence', icon: '🤖' },
  { name: 'DevOps', category: 'Methodology', icon: '🔄' },
];
---

<section class="border-b border-gray-200 bg-white py-16 dark:border-gray-800 dark:bg-gray-900 sm:py-20 lg:py-24">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-12 text-center">
      <h2 class="mb-3 text-3xl font-semibold tracking-tight text-gray-900 dark:text-gray-100 sm:text-4xl">
        {t('home.tech_stack')}
      </h2>
      <p class="text-base text-gray-600 dark:text-gray-400 sm:text-lg">
        Technologies I work with on a daily basis
      </p>
    </div>

    <div class="mx-auto grid max-w-6xl grid-cols-2 gap-6 sm:grid-cols-3 lg:grid-cols-4 lg:gap-8">
      {
        technologies.map((tech) => (
          <TechCard name={tech.name} category={tech.category} icon={tech.icon} />
        ))
      }
    </div>
  </div>
</section>
