---
import { getCollection } from 'astro:content';
import MainLayout from '@layouts/MainLayout.astro';
import { calculateReadingTime } from '@utils/helpers';

export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content, headings } = await entry.render();
const readingTime = calculateReadingTime(entry.body);

const formattedDate = new Intl.DateTimeFormat('en', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(entry.data.publishDate);
---

<MainLayout
  title={`${entry.data.title} | Sebastian Georgian Utoiu`}
  description={entry.data.description}
>
  <!-- Breadcrumb -->
  <nav class="border-b border-gray-200 bg-white">
    <div class="container mx-auto px-4 py-4 sm:px-6 lg:px-8">
      <div class="flex items-center gap-2 text-sm text-gray-600">
        <a href="/" class="hover:text-gray-900">Home</a>
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"
          ></path>
        </svg>
        <span class="text-gray-900">Blog</span>
      </div>
    </div>
  </nav>

  <div class="bg-white">
    <div class="container mx-auto px-4 py-12 sm:px-6 lg:px-8">
      <div class="mx-auto grid max-w-7xl grid-cols-1 gap-12 lg:grid-cols-[1fr_300px]">
        <!-- Main Content -->
        <article class="min-w-0 scroll-mt-24">
          <!-- Header -->
          <header class="mb-10">
            <h1
              class="mb-4 text-2xl leading-tight font-bold tracking-tight text-gray-900 sm:text-3xl md:text-4xl lg:text-5xl"
            >
              {entry.data.title}
            </h1>

            <div class="mb-4 flex flex-wrap items-center gap-4 text-sm text-gray-600">
              <time datetime={entry.data.publishDate.toISOString()}>{formattedDate}</time>
              <span>•</span>
              <span>{readingTime} min read</span>
            </div>

            {
              entry.data.tags && entry.data.tags.length > 0 && (
                <div class="flex flex-wrap gap-2">
                  {entry.data.tags.map((tag) => (
                    <span class="rounded-full bg-gray-100 px-3 py-1 text-sm font-medium text-gray-700">
                      {tag}
                    </span>
                  ))}
                </div>
              )
            }
          </header>

          <!-- Hero Image -->
          {
            entry.data.image && (
              <div class="mb-10">
                <img
                  src={entry.data.image}
                  alt={entry.data.title}
                  class="aspect-video w-full rounded-xl object-cover shadow-sm"
                />
              </div>
            )
          }

          <!-- Content -->
          <div
            class="prose prose-lg prose-headings:scroll-mt-24 prose-headings:font-bold prose-headings:tracking-tight prose-headings:!text-gray-900 prose-h1:!text-gray-900 prose-h2:mb-6 prose-h2:mt-12 prose-h2:text-3xl prose-h2:!text-gray-900 prose-h3:mb-4 prose-h3:mt-10 prose-h3:text-xl prose-h3:!text-gray-900 prose-h4:!text-gray-900 prose-h5:!text-gray-900 prose-h6:!text-gray-900 prose-p:mb-4 prose-p:leading-relaxed prose-p:text-gray-900 prose-a:font-medium prose-a:text-blue-600 prose-a:no-underline hover:prose-a:underline prose-strong:font-semibold prose-strong:text-gray-900 prose-em:text-gray-900 prose-ul:my-6 prose-li:my-2 prose-li:leading-relaxed prose-li:text-gray-900 prose-code:rounded prose-code:bg-gray-100 prose-code:px-1.5 prose-code:py-0.5 prose-code:text-sm prose-code:font-medium prose-code:text-gray-900 prose-code:before:content-none prose-code:after:content-none prose-pre:rounded-lg prose-pre:bg-gray-900 prose-pre:shadow-lg max-w-none"
          >
            <Content />
          </div>
        </article>

        <!-- Sidebar - Table of Contents -->
        <aside class="hidden lg:block">
          <div class="sticky top-24">
            <div class="rounded-lg border border-gray-200 bg-white p-6">
              <h2 class="mb-4 text-sm font-semibold tracking-wider text-gray-900 uppercase">
                On this page
              </h2>
              <nav class="space-y-2">
                {
                  headings
                    .filter((heading) => heading.depth < 4)
                    .map((heading) => (
                      <a
                        href={`#${heading.slug}`}
                        class={`block text-sm leading-relaxed transition-colors hover:text-gray-900 ${
                          heading.depth === 2 ? 'pl-0 font-medium text-gray-900' : ''
                        } ${heading.depth === 3 ? 'pl-4 text-gray-600' : ''}`}
                      >
                        {heading.text}
                      </a>
                    ))
                }
              </nav>
            </div>

            <!-- Back to Blog -->
            <div class="mt-6">
              <a
                href="/blog"
                class="inline-flex w-full items-center justify-center gap-2 rounded-lg border-2 border-gray-200 px-4 py-3 text-sm font-medium text-gray-700 transition-all hover:border-gray-900 hover:bg-gray-900 hover:text-white"
              >
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
                Back to Blog
              </a>
            </div>
          </div>
        </aside>
      </div>
    </div>
  </div>
</MainLayout>
