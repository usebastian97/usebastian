---
import { ViewTransitions } from 'astro:transitions';
import '../styles/global.css';
import type { Language } from '@i18n/utils';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  lang?: Language;
  noIndex?: boolean;
}

const {
  title = 'Sebastian Georgian Utoiu - Cloud Architect | AI | DevOps | Azure Specialist',
  description = 'Experienced Cloud Architect specializing in Azure, AI solutions, and DevOps practices. Building scalable, secure, and efficient cloud infrastructure.',
  image = '/og-image.jpg',
  lang = 'en',
  noIndex = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL(image, Astro.site);

const schema = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Sebastian Georgian Utoiu',
  jobTitle: 'Cloud Architect',
  description: description,
  url: 'https://www.sgu.com',
  sameAs: [
    'https://github.com/sgu',
    'https://linkedin.com/in/sebastian-utoiu',
    'https://twitter.com/sgu',
  ],
  knowsAbout: ['Cloud Architecture', 'Azure', 'AI', 'DevOps', 'Security'],
};
---

<!doctype html>
<html lang={lang} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    {noIndex && <meta name="robots" content="noindex, nofollow" />}

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:site_name" content="Sebastian Georgian Utoiu" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={ogImageURL} />

    <!-- Alternate Language Links -->
    <link rel="alternate" hreflang="en" href={`https://www.sgu.com${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="de" href={`https://www.sgu.com/de${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="fr" href={`https://www.sgu.com/fr${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="nl" href={`https://www.sgu.com/nl${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="zh-Hans" href={`https://www.sgu.com/ch${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="ja" href={`https://www.sgu.com/jp${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="it" href={`https://www.sgu.com/it${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="es" href={`https://www.sgu.com/es${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="pt" href={`https://www.sgu.com/pt${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="pt-BR" href={`https://www.sgu.com/pt-br${Astro.url.pathname}`} />
    <link rel="alternate" hreflang="x-default" href={`https://www.sgu.com${Astro.url.pathname}`} />

    <!-- Preload Fonts -->
    <link
      rel="preload"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Fira+Code:wght@400;500&display=swap"
      as="style"
      onload="this.onload=null;this.rel='stylesheet'"
    />

    <!-- JSON-LD Schema -->
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />

    <ViewTransitions />
  </head>
  <body class="antialiased">
    <slot />
  </body>
</html>
