---
import MainLayout from './MainLayout.astro';
import type { Language } from '@i18n/utils';

interface Props {
  title: string;
  description: string;
  publishDate: Date;
  readingTime: number;
  tags?: string[];
  lang?: Language;
}

const { title, description, publishDate, readingTime, tags = [], lang = 'en' } = Astro.props;

const formattedDate = new Intl.DateTimeFormat(lang, {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(publishDate);
---

<MainLayout title={`${title} | Sebastian Georgian Utoiu`} description={description} lang={lang}>
  <article class="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:px-8">
    <header class="mb-12">
      <h1 class="mb-4 text-4xl font-bold tracking-tight sm:text-5xl">{title}</h1>
      <div class="flex flex-wrap items-center gap-4 text-sm text-gray-600 dark:text-gray-400">
        <time datetime={publishDate.toISOString()}>{formattedDate}</time>
        <span>•</span>
        <span>{readingTime} min read</span>
      </div>
      {
        tags.length > 0 && (
          <div class="mt-4 flex flex-wrap gap-2">
            {tags.map((tag) => (
              <span class="rounded-full bg-gray-100 px-3 py-1 text-sm dark:bg-gray-800">
                {tag}
              </span>
            ))}
          </div>
        )
      }
    </header>

    <div
      class="prose prose-lg dark:prose-invert prose-headings:font-bold prose-a:text-blue-600 prose-code:rounded prose-code:bg-gray-100 prose-code:px-1 prose-code:py-0.5 prose-code:before:content-none prose-code:after:content-none dark:prose-code:bg-gray-800 max-w-none"
    >
      <slot />
    </div>
  </article>
</MainLayout>
