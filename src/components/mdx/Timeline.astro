---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div class="not-prose my-8">
  {title && <h4 class="mb-4 text-lg font-semibold text-gray-900">{title}</h4>}
  <div class="space-y-0">
    <slot />
  </div>
</div>
