---
interface Props {
  number?: number;
  title?: string;
}

const { number, title } = Astro.props;
---

<div class="step-item relative pb-8 last:pb-0">
  <div
    class="absolute -left-8 flex h-6 w-6 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-purple-600 text-xs font-bold text-white shadow-sm ring-4 ring-white"
  >
    {number || '•'}
  </div>
  <div class="pt-0.5">
    {title && <h5 class="mb-2 font-semibold text-gray-900">{title}</h5>}
    <div class="text-sm leading-relaxed text-gray-600">
      <slot />
    </div>
  </div>
</div>
